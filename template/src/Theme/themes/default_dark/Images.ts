/**
 * Images should be stored in the `src/Images` directory and referenced using variables defined here.
 */

export default function () {
  return {
    logo: {
      uri:
        'https://thecodingmachine.github.io/react-native-rctemplate/img/TOM-small.png',
    },
  }
}
